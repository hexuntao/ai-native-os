FROM node:22-bookworm-slim

ENV PNPM_HOME="/pnpm"
ENV PATH="$PNPM_HOME:$PATH"

RUN corepack enable

WORKDIR /app

COPY . .

RUN pnpm install --frozen-lockfile

EXPOSE 3040

CMD ["pnpm", "--filter", "@ai-native-os/jobs", "start"]
